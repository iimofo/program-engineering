# Тема: ТЕМА 8. Введение в ООП

- Студент: талеб мустафа хашим талеб
- Группа: ИВТ-23-1

## Таблица выполнения заданий

| Задание   | Лаб_раб | Сам_раб |
|-----------|:-------:|:-------:|
| Задание 1 |    +    |    +    |
| Задание 2 |    +    |    +    |
| Задание 3 |    +    |    +    |
| Задание 4 |    +    |    +    |
| Задание 5 |    +    |    +    |


---

## Лабораторная работа 8: Введение в ООП

### Задание 1: Создание класса "Car" 🚗

**Вывод:** Создан базовый класс **`Car`** с конструктором **`__init__`** для инициализации атрибутов **`make`** (производитель) и **`model`** (модель). Успешно создан объект этого класса.

---

### Задание 2: Добавление метода "drive" 💨
![Задание](screenshots/lab_task2.png)

**Вывод:** Класс **`Car`** дополнен **методом объекта** **`drive(self)`**, который позволяет выполнять действие, связанное с экземпляром класса. Продемонстрирован вызов метода для созданного объекта, имитирующий движение.

---

### Задание 3: Реализация наследования 🔋
![Задание](screenshots/lab_task3.png)

**Вывод:** Реализовано **наследование** от класса **`Car`** к дочернему классу **`ElectricCar`**. Дочерний класс использует **`super().__init__`** для вызова конструктора родителя и добавляет собственный атрибут **`battery_capacity`** и метод **`charge()`**.

---

### Задание 4: Реализация инкапсуляции 🔒
![Задание](screenshots/lab_task4.png)

**Вывод:** Продемонстрирована **инкапсуляция**: атрибут **`_make`** реализован как **защищенный** (по соглашению `_`), а **`__model`** как **приватный** (name mangling `__`). Показано, что к защищенному атрибуту можно обратиться напрямую, а попытка прямого доступа к приватному атрибуту вызывает ошибку `AttributeError`.

---

### Задание 5: Реализация полиморфизма ✨
![Задание](screenshots/lab_task5.png)

**Вывод:** Реализован **полиморфизм** через иерархию классов **`Shape`** -> **`Rectangle`** / **`Circle`**. Оба дочерних класса **переопределяют** метод **`area()`** в соответствии со своей логикой. В цикле продемонстрировано, как один и тот же вызов `shape.area()` выполняет разный код в зависимости от типа объекта.

---
## Самостоятельная работа: Классы и Наследование

### Задание 1: Создание класса "Course" и объекта 📚
![Задание](screenshots/cam_task1.png)

**Вывод:** Создан новый класс **`Course`** с атрибутами **`title`** и **`duration_weeks`**, отличный от примера в лаб. работе. Успешно создан и проинициализирован его объект.

---

### Задание 2: Добавление метода "start_course" ✅
![Задание](screenshots/cam_task2.png)

**Вывод:** Класс **`Course`** дополнен новым атрибутом **`is_active`** и **методом объекта** **`start_course()`**, который меняет этот атрибут и проверяет его состояние, предотвращая повторный запуск.

---

### Задание 3: Реализация наследования "OnlineCourse" 🌐
![Задание](screenshots/cam_task3.png)

**Вывод:** Реализовано **наследование** от **`Course`** к **`OnlineCourse`**. Добавлен специфический атрибут **`platform`** и метод **`publish_link()`**. Продемонстрирован вызов унаследованного метода **`get_details()`** и его переопределение.

---

### Задание 4: Инкапсуляция в классе "Student" 🔑
![Задание](screenshots/cam_task4.png)

**Вывод:** Реализована **инкапсуляция** в классе **`Student`**: атрибут **`_name`** как **защищенный**, а **`__student_id`** как **приватный**. Продемонстрирован публичный метод **`get_info()`** для безопасного доступа к приватным данным.

---

### Задание 5: Полиморфизм в расчете бонусов 💰
![Задание](screenshots/cam_task5.png)

**Вывод:** Продемонстрирован **полиморфизм** через классы **`Employee`** -> **`Manager`** / **`Programmer`**. Каждый дочерний класс **переопределил** метод **`calculate_bonus()`** для применения разной логики расчета бонусов (15% vs 10%), используя единый интерфейс вызова в общем цикле.
